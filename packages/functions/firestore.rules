rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Deny all client access - API-only architecture
    // Admin SDK (Firebase Functions) bypasses these rules
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
